# Copy the value below into the repo-root .env file (not this one — Vite is
# configured to read env vars from the monorepo root, see apps/web/vite.config.ts).
VITE_PEXELS_API_KEY=your_pexels_api_key_here
