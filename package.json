{
  "name": "fotoowl-media-sdk",
  "private": true,
  "version": "1.0.0",
  "description": "Headless media SDK ecosystem for React and React Native",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build:packages": "npm run build --workspace=@fotoowl/media-core && npm run build --workspace=@fotoowl/media-react && npm run build --workspace=@fotoowl/media-ui-react",
    "build": "npm run build:packages && npm run build --workspace=web"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
